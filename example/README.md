# phpLDAPadmin installation based on Alpine


## Example

Run:
```
docker-compose up
```

Open ```localhost``` with you favorite browser to connect with credentials:

Login DN: __cn=Manager,dc=my-domain,dc=com__

Password: __secret__
